# ホームラン競争ゲーム 開発プロンプト v2

> v2（令和8年7月30日改訂）。v1 から変わった点は §0-1 / §0-3 / §0-4 / §0-7 / §5。
> 改訂の経緯と、その時に見つかった不具合は `docs/PROGRESS.md` に残してある。

作成：令和8年7月29日 ／ 用途：Claude Code（Opus 5）に貼って開発させる

---

## 使い方

### 手順1：新しいリポジトリを用意する

```bash
mkdir -p ~/Desktop/homerun-derby && cd ~/Desktop/homerun-derby && git init
```

### 手順2：素材をコピーする（先にやること。パスに日本語が入るので後で事故る）

```bash
mkdir -p assets/src && cp "/c/Users/user/Desktop/籠田勇樹.png" assets/src/yuki_sheet.png && cp "/c/Users/user/Desktop/籠田貴也.png" assets/src/takaya_sheet.png && cp "/c/Users/user/Desktop/安藤敦司.png" assets/src/atsushi_sheet.png && cp "/c/Users/user/Desktop/13_画像・スクショ/basket.png" assets/src/faces.png && ls -la assets/src/
```

### 手順3：セッションを開始し、下の「─── ここから ───」以降を全部貼る

```bash
/effort ultracode
```

M4（描画）とM5（音・演出）は並列が効く。M0〜M3は逐次。終わったら `/effort high` に戻す。

### 手順4：必ず自分で挟まる場所

- **SPEC.md 承認時**（作るものが違ったらここで止める）
- **M0完了時**：切り出した21枚の一覧スクショを見る。ここでズレていたら以降が全部崩れる
- **M2完了時**：実際に打つ。気持ちよくなければ以降の工数は無駄になる
  - 調整ダイヤルは3つだけ → ①ミートカーソル半径 ②タイミング判定の許容フレーム数 ③ヒットストップ長さ

---

─── ここから（以下を全文コピーして貼る） ───

あなたはゲーム開発を担当する。以下の憲法に従い、完成まで作り切ること。

# 0. 作るもの：ホームラン競争ゲーム「BASKET HOME RUN DERBY」

ブラウザで動く2Dホームラン競争ゲーム。狙うのは3Dの見た目ではなく、
**ミートカーソル位置 × スイングタイミング × 球種/コース → 打球結果、の因果が明快で理不尽がないこと**。
実在の商用ゲームの画像・音声・UIデザインを一切複製しないこと。

## 0-1. 競技ルール

- 1ラウンド = **10アウト制**。ホームラン以外の打球（ファウル・凡打・空振り）はすべて1アウト
- スコア = Σ( 100 + max(0, 飛距離m − 120) × K_dist ) × コンボ倍率
  - `K_dist` の初期値は 2.0。コンボ倍率は連続HR2本目から ×1.1、以降 +0.1、上限 ×2.0。アウトでリセット
  - **K_distとコンボ上限は §3-6 のバランス実測で調整してよい唯一のスコア係数**
- **ホームランでない打球にも飛距離点が入る**（CLASSIC は飛距離×0.45、ARCADE は×0.8）。
  アウトになることは変わらない。
  これが無いと、初期能力（§0-4）の打者は 74〜86m しか飛ばず1本も入らないため、
  経験値がゼロのままレベルが上がらず**成長が始まらない**。
  パワプロのホームランアタックが「飛距離や連打で得点が高まる」設計なのも同じ理由
- **フォークは必ずボール**で、振れば必ず空振りになる。見送ると +120（見極めボーナス）。
  唯一「押さない」ことが正解になる場面で、1ボタン制における判断の置き場所
- モード
  - (a) CPU対戦：能力の異なるCPU3体と総当り
  - (b) ローカル2P交代制
  - (c) タイムアタック／自己ベスト更新
- 記録：最多本数・最長飛距離・最高スコアを localStorage に保存

## 0-2. 視点とカメラ（素材に従属させる。ここを間違えると素材が死ぬ）

**メイン視点＝サイドビュー（クォータービュー）。** 打者を画面の左〜中央下に配置する。

| フェーズ | カメラ |
|---|---|
| 投球〜スイング | 打者を横〜斜め前から見る固定カメラ。ボールは画面奥から手前へ来る（スケール拡大で奥行きを出す） |
| インパクト直後 | ヒットストップ2〜3フレーム＋軽いズーム |
| 打球飛行 | 打球追従カメラへ切替。打者は画面外に出てよい。スタジアムとフェンスが見える |
| 着弾 | スローモーション＋飛距離テロップ |

- 素材のスイング絵の向きが画面構成と合わない場合は、**水平反転（scale(-1,1)）して使ってよい**。
  反転すると背番号やロゴの文字が鏡像になるので、その場合はプレイ中の打者に文字が出ない
  コマを選ぶか、文字部分を描画しない
- 打者の向きが自然に見えるかは**必ずブラウザのスクリーンショットで自分の目で確認する**こと。
  数値では判定できない項目である

## 0-3. 打撃システム

- **入力はスイングボタン1つだけ**（画面のどこをタップしても振る）。
  ミートカーソルは廃止した。バットは常にボールに合う位置へ出るので **ミート誤差 e は常に 0** で、
  **タイミングがゲームのすべて**になる。
  固定の狙点を置く案は却下：コーナーの球がタイミングに関係なく打てなくなるため
- **難度は球速差が担う**。150 と 110 では到着時間が 147ms 違い、それが読むべき唯一の情報
- **判定**：ジャストミート / 芯外し / 差し込まれ(t<0) / 泳ぐ(t>0) / 空振り / ファウル
- **球種は4つ**：ストレート150 / チェンジアップ110 / カーブ130 / フォーク140 [km/h]。
  実際の球速はこれに **相手ピッチャーの倍率 × レベルの倍率**（上限1.15）が掛かる。
  Lv1 のルーキー戦で 101km/h、Lv99 の守護神戦で 168km/h
- **打球生成**（乱数で誤魔化さず、入力から因果を説明できる式にすること）
  - ミート誤差 `e` ＝ ゾーン面上でのカーソル中心とボール位置の距離
  - タイミング誤差 `t` ＝ スイング時刻 − 最適時刻
  - 初速 `v0 = f(パワー, e, t)`
  - 打ち出し角 `θ = f(弾道パラメータ, ボールの高さ, カーソルのY位置)`
  - 方向 `φ = f(t)`（早い＝引っ張り、遅い＝流し）
  - バックスピン `ω = f(カーソルのY位置, θ)`
- **初速の天井は 197.8 km/h（実測世界記録）で、これは調整可能なダイヤルではない。**
  バットや特殊能力は初速そのものではなく**天井を含めた最大値**を動かすこと。
  値の方を掛けると、少しタイミングを外したスイングと完璧なスイングが
  同じ初速にクランプされ、§3-4 の単調性が**画面上は何も起きないまま**壊れる。
  実際に2回起きている（`docs/PROGRESS.md` ★要判断12 と M4.0）

## 0-4. 選手（3名・固定）

**この3名は実在の従業員をモデルにしており、本人の許可を得ている。**
揶揄・侮辱的な演出には使わないこと（失敗時も落ち込む程度に留め、罵倒・爆発・過度な戯画化はしない）。
胸と背中のバスケットグループのロゴは自社ロゴであり使用可。改変しないこと。

**能力は固定値ではなく、レベルで育つ。** 表記はパワプロに合わせ、
**数値（1〜100）とランク（G〜S）の両方**を出す。

【確定】ランク対応表（パワプロアプリ／GameWith「選手能力とは？」令和8年7月30日閲覧）

| ランク | S | A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|---|---|
| 能力値 | 90–100 | 80–89 | 70–79 | 60–69 | 50–59 | 40–49 | 20–39 | 1–19 |

**F と G だけ20点幅**で、D以上は10点幅。転記ミスではなく実際の表がそう。弾道は 1〜4。

### 初期値（Lv.1）と到達点（Lv.99）

初期値は **G / F / E のみ**（E がスタート時の最大）。**弾道は全員 1 から**。

| ID | 選手名 | 背番号 | 初期ミート | 初期パワー | 到達ミート | 到達パワー | 型 |
|---|---|---|---|---|---|---|---|
| `yuki` | 籠田 勇樹（代表取締役・40代前半） | 3 | **G 14** | **E 44** | 72 | 100 | パワー型 |
| `takaya` | 籠田 貴也（管理者・30代後半） | 1 | **F 33** | **F 31** | 88 | 86 | バランス型 |
| `atsushi` | 安藤 敦司（相談支援専門員・48歳） | 7 | **E 45** | **G 17** | 100 | 70 | ミート型 |

- 到達点は3名バラバラにする。全員 100/100 に収束させると、初期の差が
  「育てば消える一時的な不便」になってしまい、個性ではなくなる
- **能力は保存せず、経験値から毎回導出すること。** セーブに書けるのは経験値だけなので、
  「育てようがない選手」を記述できるセーブが存在せず、曲線を調整しても移行処理が要らない。
  バットと相手ピッチャーも同じ理由でレベルから再計算する
- **セーブは選手ごと**（サクセス方式）。敦司を選ぶのは敦司のセーブを開くこと

### 特殊能力（レベルで解放）

**パワー系の3つは1枠**で、**最良の1つだけが有効**（同時にはつかない）。
広角打法は別枠なので、最終的に2つ持つ。

| 能力 | 解放Lv | 枠 | 効果 |
|---|---|---|---|
| パワーヒッター | 20 | パワー系 | パワー **+5**。打ち出し角が一番飛ぶ角度に40%寄る |
| 広角打法 | 32 | 別枠 | **振り遅れても詰まらない。**流し打ちの打球が伸びる |
| アーチスト | 45 | パワー系 | パワー **+10**。一番飛ぶ角度に65%寄る |
| 世界のワン | 80 | パワー系 | パワー **+15**。一番飛ぶ角度に90%／獲得経験値 ×1.35 |

- **パワーの実効上限は 115**（表示は100止まり）。100でクランプすると、
  世界のワンを持つ可能性が最も高い Lv99 の打者に対して **+15 が消える**
- **「角度を足す」実装は禁止。** 弾道4（28°）に+10°すると40°になり最適角を越え、
  **最強能力を取ると飛距離が落ちる**。「一番飛ぶ角度へ寄せる」実装にすること
- **最適打ち出し角は初速で変わる**（実測：144km/h→31°、187km/h→27°）。
  しかもバットが初速を上げると最適角は下がるので、**パワーではなく実際の初速**を見ること
- 名称は **「世界のワン」**。公開URLで配信するため、実在の選手名は使わない（§5）

### バット（20本・5レベルごと）

Lv.1 / 5 / 10 / … / 95 で1本ずつ。3つの軸（飛距離・タイミング・経験値）のトレードで、
**最後の1本以外は単純上位互換にしない**。
「あるレベルで所持している中に、全軸で他を上回る1本が存在しない」ことをテストで守る。

### 相手ピッチャー（4人・選択制）

強いほど球が速く、獲得経験値が高い。パワプロのホームランアタックの球場選択
（大きいほど高得点だが打ちにくい）と同じ構造。

| 相手 | 解放Lv | 球速倍率 | 経験値 |
|---|---|---|---|
| ルーキー | 1 | 0.78 | ×1.00 |
| セットアッパー | 8 | 0.90 | ×1.40 |
| エース | 25 | 1.00 | ×1.90 |
| 守護神 | 55 | 1.12 | ×2.60 |

### バランス目標（合格条件）

- **キャリアを通じて飛距離が単調増加すること。**レベルが上がって飛ばなくなる、
  能力を取ると弱くなる、バットが公称値を出さない——**この3つは実際に起きた**。
  いずれも画面では気付けない（40レベル前の飛距離を覚えている人はいない）ので、
  全選手・全レベル・全バットで飛距離を実測するテストで守ること
- Lv.1 ではフェンスに届かず、Lv.99＋最強バットでは 140m 前後に届くこと
- 3名の差は最後まで残すこと

## 0-7. 提供形態（外出先のスマートフォンで遊べること）

- 縦画面固定・全画面。ブラウザではなくアプリのように見えること
- **Service Worker で全ファイルを端末に保存し、電波が無くても動くこと。**
  `cache.addAll` は原子的なので、1ファイルでも欠けると**何も保存されず、
  外出先で起動しない。しかも自宅では正常に動くので気付けない**。
  precache 一覧は手書きにせず生成し、テストで実在と網羅を検査すること
- 弱い電波では `fetch` が reject せずハングするので、一定時間で見切ってキャッシュへ切り替えること
- 公開URLで配信する。**公開に伴い、実在の選手名は使わない（§5）**

## 0-5. 画像素材（`assets/src/` に配置済み。外部から取得しない）

| ファイル | サイズ | 内容 |
|---|---|---|
| `yuki_sheet.png` | 1774 × 887 | 籠田勇樹のバッティングシート |
| `takaya_sheet.png` | 1774 × 887 | 籠田貴也のバッティングシート |
| `atsushi_sheet.png` | **1672 × 941** | 安藤敦司のバッティングシート |
| `faces.png` | 1086 × 1448 | 3名の顔・表情シート（漫画LP用の人物シート） |

### バッティングシートの構造（3枚共通の構図・座標は共通ではない）

各シートは横に3パネル並ぶ：

1. **左パネル**：構え（全身1体）
2. **中央パネル**：スイング連続5コマ（構え → テイクバック/足上げ → インパクト → フォロー → フォロースルー）
3. **右パネル**：背面（打席の後ろ姿。背番号とローマ字表記が見える）

**重要：`atsushi_sheet.png` だけ画像サイズが異なる（1672×941）。**
3枚に共通の切り出し座標を使ってはならない。**必ず画像ごとに座標を実測して決めること。**

### 切り出し仕様（M0で実施）

Python の Pillow で切り出し、`assets/player/{id}/{pose}.png` として保存する。

- `pose` = `stance`（構え）／`swing_0`〜`swing_4`（5コマ）／`back`（背面）
- 1選手7ポーズ × 3名 = **21枚**
- **背景は透過処理すること。** 画像の四辺の外周からフラッドフィル（連結領域の塗り分け）で
  背景色を除去する。色距離の閾値は**画像ごとに個別に決める**（3枚とも背景の色味とラインの入り方が違う）
  - **絶対に守ること：ユニフォームの白いパンツを削らないこと。**
    外周からの連結フラッドフィルを使えば内部の白は保護される。
    「白ピクセルを一括で透明化する」実装は禁止
  - 貴也・敦司のシートには背景に薄い内野ラインが描かれている。閾値が狭いとラインが残る
- `takaya_sheet.png` のフォローのコマと `atsushi_sheet.png` のインパクトのコマには
  **絵の中にボールが描き込まれている**。ゲームは別途ボールを描画するので二重に見える可能性がある。
  切り出し後に確認し、目立つようなら該当コマの描き込みボールを消すか、
  そのコマではゲーム側のボール描画を1フレーム抑制すること
- **元の `assets/src/*.png` は読み取り専用として扱い、絶対に上書き・改変しない**

### 顔素材（`faces.png`）の使い道

顔は**カットイン専用**。プレイ中の打者には使わない（全身素材があるため）。
シートには「顔の向き3種（正面/ななめ/横顔）」「表情4種（笑顔/真剣/安心・ほっと/考えている）」が
3名分含まれる。同じくPillowで切り出し `assets/face/{id}_{variant}.png` に保存する。

| 場面 | 使う表情 |
|---|---|
| 選手選択画面 | 正面・笑顔 |
| ホームラン時カットイン | 笑顔 |
| 空振り・アウト | 考えている |
| 自己ベスト更新・140m超の特大弾 | 安心・ほっと |
| リザルト・順位表 | ななめ・真剣 |

選手選択画面には各選手の人物像（下記）をフレーバーテキストとして載せること。

- 籠田 勇樹：穏やかで情熱的。人の話を丁寧に聞き、その人らしさを大切にするリーダー
- 籠田 貴也：誠実で責任感が強い。細かな気配りができ、チームをまとめる頼れる存在
- 安藤 敦司：明るく前向きで、話しやすい雰囲気。一緒に将来のプランを考えるのが得意

## 0-6. 演出（手応えの半分はここ）

- ジャストミート時にヒットストップ 2〜3フレーム
- 打球音を WebAudio で合成し打ち分ける（ジャスト／芯外し／詰まり／空振り）
- 飛距離テロップ、140m超は特大弾演出、コンボ表示
- 打席入場時に背面図を使った演出を入れる（背番号とローマ字が見えるので映える）

---

# 1. 技術制約（交渉不可）

- TypeScript + HTML5 Canvas 2D。ビルドは `tsc` のみ（バンドラ・フレームワーク・3Dライブラリ禁止）
- ランタイム依存パッケージ 0個。devDependencies は typescript のみ
  （切り出しスクリプトの Pillow はビルド外のツールなので可）
- テストは Node標準の `node --test`
- 音はすべて WebAudio 合成。画像は `assets/` に配置済みのローカルPNGのみ使用可。
  **ネットワークから画像・音声・フォントを取得することを禁止する。** それ以外はコード描画
- **内部は3D座標（x=一塁方向, y=上, z=センター方向、原点=本塁）で計算し、描画時に透視投影する**

# 2. アーキテクチャ（交渉不可）

```
src/core/          … ゲームロジック。純粋関数のみ。
                     DOM・Canvas・Date.now・Math.random を import もコールも一切しない。
                     乱数はシード付きPRNGを自作し state に持たせる
src/core/physics.ts… 打球飛行。空気抵抗＋マグヌス力を含む数値積分（dt = 1/240s 固定）
src/render/        … 描画・カメラ・投影・スプライト。state を読むだけ。書き換えない
src/audio/         … WebAudio合成。state を読むだけ
src/input/         … 入力をコマンドに変換するだけ
src/main.ts        … 固定タイムステップで上記を繋ぐ
tools/             … 切り出しスクリプト・検証用HTML（本体にバンドルしない）
```

中核API：`step(state: GameState, cmd: Command): GameState`
同じ `(state, cmd)` からは常に同じ結果が出ること。崩れたら設計を直す。

# 3. 検証（これを満たすまで「完了」と言わない）

毎マイルストーンで下記を**実際に実行し、出力を貼って**報告する。

1. `npx tsc --noEmit` … エラー0
2. `node --test` … 全通過。コアのテストは実装より先に書く
3. **物理の妥当性テスト**（`scripts/physics-check.ts`）— 以下を数値で出す
   - 初速180km/h・打ち出し角30°・標準バックスピンでの飛距離
   - 各選手のパワー値での最長飛距離
   - 現実のプロ野球の飛距離の目安と照合し、ずれていれば空気抵抗係数・マグヌス係数を調整する。
     **目安値は自分で一次情報を確認すること**（私が口頭で言った数字は未検証である）
4. **単調性テスト**（自動）— 他条件を固定して次が成り立つこと
   - パワーを上げたら飛距離は減らない
   - ミート誤差を増やしたら飛距離は増えない
   - タイミング誤差 |t| を増やしたら飛距離は増えない
   - 破れたら打球生成式にバグがある
5. **ヘッドレスfuzz**（`scripts/smoke.ts`）— シード100通り × 各10,000スイングを乱数入力で実行。
   合格条件＝例外0・NaN0・飛距離が負にならない・打球が地面下に潜らない・状態不変条件を全ステップ維持
6. **難易度とバランスの実測**（`scripts/difficulty.ts`）— 自動プレイAIを3段階作り、
   **3選手 × 3段階** の HR率・平均飛距離・平均スコアを表で出す
   - 入力精度：完璧 / タイミング±2フレームずれ / ±5フレームずれ
   - 目標：完璧で HR率8割前後、±5で1〜2割程度。かつ**3選手のスコア期待値が ±15% 以内**
   - 数値は必ず報告し、目標に届かない場合の調整案を私に提示すること
7. **スプライト検証**（`tools/sprite-check.html`）— 切り出した21枚＋顔素材を
   **市松模様の背景**（透過の抜けが見えるように）で並べた一覧を生成し、
   preview_start ＋スクリーンショットで自分の目で確認する。次を確認して報告すること
   - 人物の取り違えがないか（勇樹＝眼鏡なし黒髪／貴也＝眼鏡／敦司＝茶髪）
   - 見切れ・余白の混入・隣のコマの写り込みがないか
   - 背景が抜けているか。**白いユニフォームのパンツが削れていないか**
   - スイング5コマが正しい順序か
8. **ブラウザ実機** — `.claude/launch.json` を作り preview_start でサーバを立て、
   実際に操作してスクリーンショットを撮り、console エラー0件を確認する。
   スイングアニメが自然に見えるか、打者の向きと打球方向が矛盾していないかを目視で判定する

# 4. 進め方

最初に `docs/SPEC.md`（データ構造・打球生成式・球種テーブル・選手能力表・固有スキル・
スタジアム寸法・スコア式・切り出し座標表）と `docs/PROGRESS.md`（マイルストーンとチェックリスト）
を書き、**私の承認を取って止まること**。承認後は以下を順に進め、各段階で §3 の該当項目を報告して次へ進む。

| # | 内容 | 完了条件 |
|---|---|---|
| M0 | 型定義・PRNG・テスト基盤・**素材の切り出しと透過** | §3-7 のスプライト一覧を私が承認 |
| M1 | **打球物理だけ**を完成（画面なし。テストと数値だけで正しさを確定） | §3-3、§3-4 が通る |
| M2 | 投球→カーソル→スイング→打球生成。素朴な描画で1球打てる | 実機で打てる。私が「気持ちいい」と言う |
| M3 | 10アウトのラウンドが成立。スコア・コンボ・記録保存 | §3-5 が通る |
| M4 | サイドビュー描画・打球追従カメラ・スタジアム・HUD・テロップ | §3-8 が通る |
| M5 | 音（WebAudio合成）・ヒットストップ・スロー・カットイン | 実機確認 |
| M6 | CPU対戦・2P・選手選択・タイトル/リザルト画面 | 実機確認 |
| M7 | バランス調整 | §3-6 の目標を満たす |

各マイルストーン完了時に `docs/PROGRESS.md` を更新し git commit する（1マイルストーン1コミット以上）。

**M0 と M1 を飛ばして描画に進んではならない。**
素材が正しく切り出せていること、物理が正しいことを先に確定させる。
ここを曖昧にしたまま先に進むと、後半で「なんか飛ばない」「絵がズレる」となって全部やり直しになる。

# 5. 禁止事項

- 検証を実行せずに「完成しました」「動くはずです」と報告すること
- テストが落ちたときに、テスト側を緩めて通すこと
- `docs/SPEC.md` にない機能を勝手に足すこと（提案は歓迎。実装前に聞く）
- コアに乱数・時刻・DOM を持ち込むこと
- 分からない箇所を推測で埋めること（分からないなら「未確認」と書いて聞く）
- 打球結果を「それらしい乱数」で誤魔化すこと。**入力と結果の因果が説明できる式にすること**
- **3枚のバッティングシートに共通の切り出し座標を使うこと**（サイズが違う）
- **§0-4 の初期値・到達点・特殊能力の数値を、経営者に断らず変更すること**
  （v1 では「基礎ランクを変更するな」だった。能力が育つ設計になったので、
  固定すべき対象が「ランク」から「初期値と到達点と成長曲線」に変わった）
- 元素材 `assets/src/*.png` を上書き・改変すること
- 実在の選手名・球団名・リーグ名・ロゴを使うこと。既存ゲームの画像・音声・UIを複製すること
  - **公開URLで配信するので、これは実務上のリスクでもある。**
    特殊能力「世界のワン」は実在の選手を指す通称を避けた名称であり、戻してはならない
- 報酬（レベル・特殊能力・バット）を、**飛距離を実測せずに**追加・変更すること。
  「取ると弱くなる」「公称値が出ない」は3回起きていて、いずれも画面では気付けなかった

# 6. 報告フォーマット（毎マイルストーン）

- 実装したもの（ファイル名つき）
- 検証結果の実際の出力（数値をそのまま。要約しない）
- 既知の不具合・未実装（隠さない）
- 次に何をするか / 私に決めてほしいこと

─── ここまで ───

---

## 付録：この設計が崩れる条件

1. **スイング5コマの向きが、どう配置しても画面と噛み合わない場合**
   AI生成イラストのため、厳密な野球中継の視点とは対応していない可能性がある。
   その場合はサイドビューを諦め、**打者を画面下部中央に置く「打者を正面から見る視点」**に切り替える。
   ボールは画面奥から手前に飛来し、打った瞬間にカメラが打球追従へ切り替わる構成なら成立する。
   判断はM4のスクリーンショットで行う（M2の段階では素朴な描画なので判定できない）

2. **バランスが ±15% に収束しない場合**
   基礎能力ランクが指定固定なので、調整の自由度は固有スキルとスコア係数しかない。
   収束しない場合の選択肢は3つ ——
   (a) スコア式を「本数のみ・飛距離はタイブレーク」に単純化する
   (b) アウト数を選手ごとに変える（ミートEの勇樹は12アウト等）
   (c) 基礎ランクの再指定を経営者に求める
   どれも経営者判断が要るので、AIに勝手に選ばせない

3. **「気持ちよさ」は自動検証できない**
   §3-6 は「HR率が何%か」までしか出せない。その%が楽しいかは打って決めるしかない。
   M2の実機確認だけは、人間が必ず挟まる工程である

## 付録：確度ラベル

- 【確定】画像4枚の実サイズ・3パネル構成・背番号（3/1/7）・ローマ字表記（YUKI/TAKAYA/ATSUSHI）
  … 実ファイルを読んで確認済み
- 【確定】`drafts/` は .gitignore のホワイトリスト方式で追跡対象外
  … `git check-ignore` で確認済み（実名を含む本ファイルはGitHubへ push されない）
- 【要確認】プロ野球のホームラン飛距離の目安（平均・最長）
  … 私の記憶ベースで一次情報の裏取りをしていない。物理係数の調整前に開発セッション側で確認させる
- 【未確認】スイング5コマの向きが実装時の画面構成と噛み合うか
  … M4のスクリーンショットで判定する
